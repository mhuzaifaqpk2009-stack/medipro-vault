import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { toast } from "sonner";
import { Settings as SettingsIcon, Save, Plus, Trash2, Pencil, Users as UsersIcon, ShieldCheck, AlertTriangle, RotateCcw, HardDriveDownload, HardDriveUpload, FolderOpen, Keyboard, Bell, Search } from "lucide-react";
import { useProjectStore } from "@/store/project-store";
import { useSession } from "@/store/session-store";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
import { Switch } from "@/components/ui/switch";
import { Checkbox } from "@/components/ui/checkbox";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { QUICK_ACTIONS, effectiveActionHotkey } from "@/lib/quick-actions";
import { DeploymentSettings } from "@/components/settings/DeploymentSettings";
import { AdminGate } from "@/components/PermissionGate";
import { PasswordInput } from "@/components/PasswordInput";
import { ResetSetupDialog } from "@/components/ResetSetupDialog";
import { effectiveHotkey } from "@/routes/app";
import { visibleNavItems } from "@/lib/nav";
import { comboFromEvent } from "@/lib/hotkeys";
import { pickBackupFolder, readBackupFile } from "@/lib/local-store";
import { runBackupNow } from "@/lib/backup";
import { restoreFromBytes } from "@/store/project-store";
import { WrongPasswordError } from "@/lib/project-codec";
import { askPassword } from "@/components/PasswordPromptDialog";
import { readInstall, createUser, upsertUser, removeUser, hashPassword } from "@/lib/install";
import { defaultPermissions, permissionsForTemplate, ROLE_TEMPLATE_LABELS, type StoredUser, type UserPermissions, type RoleTemplate } from "@/lib/users";
import { NOTIFICATION_TYPES, NOTIFICATION_LABELS, DEFAULT_NOTIFICATION_PREFERENCES, type NotificationType } from "@/lib/notification-types";
import { KeyboardSettingsPanel } from "@/components/KeyboardSettingsPanel";

export const Route = createFileRoute("/app/settings")({ component: () => <AdminGate><SettingsPage /></AdminGate> });
type PermissionAction = { key: keyof UserPermissions; label: string };
type PermissionPanel = { key: keyof UserPermissions; label: string; actions?: PermissionAction[] };
const PERMISSION_PANELS: PermissionPanel[] = [
  { key: "medicines", label: "Medicines panel", actions: [{ key: "medicinesAdd", label: "Add" }, { key: "medicinesEdit", label: "Edit" }, { key: "medicinesDelete", label: "Delete" }] },
  { key: "sales", label: "Sell panel", actions: [{ key: "applyDiscount", label: "Apply discount" }, { key: "forceSale", label: "Force sale" }, { key: "changeCheckoutPrice", label: "Change item price" }] },
  { key: "prescriptions", label: "Prescription panel", actions: [{ key: "prescriptionsAdd", label: "Add" }, { key: "prescriptionsEdit", label: "Edit" }, { key: "prescriptionsDelete", label: "Delete" }, { key: "prescriptionsPrint", label: "Print" }, { key: "prescriptionsLoadToSale", label: "Load to sale" }] },
  { key: "bills", label: "Bills panel" }, { key: "inventory", label: "Inventory panel" },
  { key: "stocktake", label: "Stock Take panel" },
  { key: "racks", label: "Rack panel", actions: [{ key: "racksAdd", label: "Add racks" }] },
  { key: "operations", label: "Pharmacy operations", actions: [{ key: "operationsBarcode", label: "Barcode" }, { key: "operationsBatches", label: "Batch + FEFO" }, { key: "operationsReturns", label: "Returns" }, { key: "operationsCustomerLedger", label: "Customer ledger" }, { key: "operationsSupplierLedger", label: "Supplier ledger" }] },
  { key: "purchases", label: "Purchase panel", actions: [{ key: "purchasesAdd", label: "Add" }, { key: "purchasesEdit", label: "Edit" }, { key: "purchasesDelete", label: "Delete" }] },
  { key: "suppliers", label: "Supplier panel", actions: [{ key: "suppliersAdd", label: "Add" }, { key: "suppliersEdit", label: "Edit" }, { key: "suppliersDelete", label: "Delete" }] },
  { key: "customers", label: "Customer panel", actions: [{ key: "customersAdd", label: "Add" }, { key: "customersEdit", label: "Edit" }, { key: "customersDelete", label: "Delete" }] },
  { key: "patients", label: "Patients panel" },
  { key: "categories", label: "Category panel", actions: [{ key: "categoriesAdd", label: "Add" }, { key: "categoriesEdit", label: "Edit" }, { key: "categoriesDelete", label: "Delete" }] },
  { key: "reports", label: "Report panel", actions: [{ key: "reportsPrint", label: "Print" }, { key: "reportsExport", label: "Export" }] },
  { key: "messages", label: "Messages panel", actions: [{ key: "messagesToAdmin", label: "Send messages to Admin" }] },
  { key: "notifications", label: "Notifications & pinned panel", actions: [{ key: "viewNotifications", label: "View notifications" }, { key: "viewPinPanel", label: "View pinned" }, { key: "pinPanel", label: "Pin / unpin" }] },
];

function RackAccessSettings(){
  const racks=useProjectStore(s=>s.data?.settings.racks ?? []);
  const [tick,setTick]=useState(0);
  const users=readInstall()?.users ?? [];
  function toggle(u:StoredUser,name:string,on:boolean){ const next={...u,allowedRackNames:on?[...(u.allowedRackNames??[]).filter(x=>x!==name),name]:(u.allowedRackNames??[]).filter(x=>x!==name)}; upsertUser(next); setTick(x=>x+1); }
  return <section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Rack access</h2><p className="mb-4 text-xs text-muted-foreground">Assign each staff member the pharmacy-defined racks they are allowed to access. Admins always have full access.</p>{users.filter(u=>u.role!=="admin").map(u=><div key={u.id} className="mb-4 rounded-lg border p-4"><div className="mb-2 font-medium">{u.username}</div><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{racks.filter(r=>r.active).map(r=><label key={r.id} className="flex items-center gap-2 text-sm"><Checkbox checked={(u.allowedRackNames??[]).includes(r.name)} onCheckedChange={v=>toggle(u,r.name,!!v)}/>{r.name}</label>)}</div>{racks.filter(r=>r.active).length===0&&<p className="text-xs text-muted-foreground">No racks have been created yet.</p>}</div>)}{users.filter(u=>u.role!=="admin").length===0&&<p className="text-sm text-muted-foreground">No non-admin users.</p>}<span className="sr-only">{tick}</span></section>
}

function SettingsPage() {
  const navigate = useNavigate(); const data = useProjectStore((s) => s.data)!; const mutate = useProjectStore((s) => s.mutate); const save = useProjectStore((s) => s.save); const s = data.settings;
  const currentUser = useSession((st) => st.user); const setSessionUser = useSession((st) => st.setUser); const [usersTick, setUsersTick] = useState(0); const users = readInstall()?.users ?? [];
  const [editing, setEditing] = useState<StoredUser | null>(null); const [showNew, setShowNew] = useState(false); const [capturing, setCapturing] = useState<string | null>(null); const [capturingAction, setCapturingAction] = useState<string | null>(null); const [backupBusy, setBackupBusy] = useState(false); const [showReset, setShowReset] = useState(false); const [notificationSearch, setNotificationSearch] = useState("");
  const set = <K extends keyof typeof s>(key: K, value: (typeof s)[K]) => mutate((d) => { (d.settings as any)[key] = value; });
  useEffect(() => { if (!capturing) return; const onKey = (e: KeyboardEvent) => { if (e.key === "Escape") { setCapturing(null); return; } const combo = comboFromEvent(e); if (!combo) return; e.preventDefault(); e.stopPropagation(); mutate((d) => { d.settings.tabHotkeys = { ...(d.settings.tabHotkeys ?? {}), [capturing]: combo }; }); setCapturing(null); toast.success(`Shortcut set to ${combo}`); }; window.addEventListener("keydown", onKey, true); return () => window.removeEventListener("keydown", onKey, true); }, [capturing, mutate]);
  useEffect(() => { if (!capturingAction) return; const onKey = (e: KeyboardEvent) => { if (e.key === "Escape") { setCapturingAction(null); return; } const combo = comboFromEvent(e); if (!combo) return; e.preventDefault(); e.stopPropagation(); mutate((d) => { d.settings.actionHotkeys = { ...(d.settings.actionHotkeys ?? {}), [capturingAction]: combo }; }); setCapturingAction(null); toast.success(`Shortcut set to ${combo}`); }; window.addEventListener("keydown", onKey, true); return () => window.removeEventListener("keydown", onKey, true); }, [capturingAction, mutate]);
  async function doBackupNow(chooseFolder = false) { setBackupBusy(true); try { const written = await runBackupNow({ chooseFolder }); if (!written) return; toast.success(`Backup saved: ${written}`); } catch (e: any) { toast.error(e?.message ?? "Backup failed"); } finally { setBackupBusy(false); } }
  async function doLoadBackup() { if (!window.confirm("Load a backup file? This replaces all current data on this computer.")) return; setBackupBusy(true); try { const bytes = await readBackupFile(); if (!bytes) return; try { await restoreFromBytes(bytes); } catch (e) { if (e instanceof WrongPasswordError) { const pw = await askPassword("Enter password", "This backup file is locked. Enter its password."); if (!pw) return; await restoreFromBytes(bytes, pw); } else throw e; } toast.success("Data restored from backup"); sessionStorage.setItem("medicore.resume", "1"); setTimeout(() => window.location.reload(), 400); } catch (e: any) { toast.error(e?.message ?? "Could not load backup"); } finally { setBackupBusy(false); } }
  async function chooseAutoBackupFolder() { const folder = await pickBackupFolder(); if (!folder) { toast.error("Choosing a folder is only available in the desktop app"); return; } set("autoBackupFolder", folder); toast.success("Backup location updated"); }
  async function toggleAutoBackup(v: boolean) { if (!v) { set("autoBackupEnabled", false); return; } let folder = s.autoBackupFolder; if (!folder) { const picked = await pickBackupFolder(); if (!picked) { toast.error("Choose a backup folder to enable auto backup"); return; } folder = picked; } mutate((d) => { d.settings.autoBackupFolder = folder; d.settings.autoBackupEnabled = true; }); toast.success("Auto backup enabled"); }
  function clearBillHistory() { if (!window.confirm("Are you sure you want to delete all bill history? This cannot be undone.")) return; if (!window.confirm("This will permanently remove ALL sales/invoices. Continue?")) return; mutate((d) => { d.sales = []; }); toast.success("Bill history cleared"); }
  const notificationRows = NOTIFICATION_TYPES.filter((t) => `${NOTIFICATION_LABELS[t]} ${t}`.toLowerCase().includes(notificationSearch.trim().toLowerCase()));
  function notificationEnabled(type: NotificationType) { return s.notificationPreferences?.[type] ?? DEFAULT_NOTIFICATION_PREFERENCES[type]; }
  function setNotification(type: NotificationType, value: boolean) { mutate((d) => { d.settings.notificationPreferences = { ...(d.settings.notificationPreferences ?? {}), [type]: value }; }); }
  function resetNotifications() { mutate((d) => { d.settings.notificationPreferences = { ...DEFAULT_NOTIFICATION_PREFERENCES }; }); toast.success("Notification defaults restored"); }

  return <div className="mx-auto max-w-4xl p-6 md:p-10">
    <header className="mb-6 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-gradient-to-br from-primary to-primary-glow text-primary-foreground shadow-soft"><SettingsIcon className="h-5 w-5" /></div><div><h1 className="font-display text-2xl font-bold tracking-tight">Settings</h1><p className="text-sm text-muted-foreground">Admin-only. Manage pharmacy details, users, and workspace behaviour.</p></div><Button onClick={async () => (await save()) && toast.success("Saved")}><Save className="mr-2 h-4 w-4" /> Save now</Button></header>
    <Tabs defaultValue="users"><TabsList className="mb-4 flex w-full gap-1.5 overflow-x-auto rounded-xl p-1.5"><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="users">Users</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="rackaccess">Rack Access</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="general">Pharmacy & billing</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="workspace">Workspace</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="network">Computer mode</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="notifications">Notifications</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="data">Backup & data</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="shortcuts">Shortcuts</TabsTrigger><TabsTrigger className="min-h-10 shrink-0 whitespace-nowrap px-3 text-sm font-medium" value="danger">Danger zone</TabsTrigger></TabsList>
      <TabsContent value="users"><section className="surface-card p-6"><div className="mb-4 flex items-center gap-2"><UsersIcon className="h-4 w-4 text-primary" /><h2 className="font-display text-base font-semibold">Users</h2><Button size="sm" className="ml-auto" onClick={() => setShowNew(true)}><Plus className="mr-1.5 h-4 w-4" /> Add user</Button></div><p className="mb-4 text-xs text-muted-foreground">Each user signs in with their own username & password. Admins have full access; other users get only the permissions you set below.</p><div className="divide-y rounded-md border">{users.map((u) => <div key={u.id} className="flex items-center gap-3 p-3"><div className={`grid h-8 w-8 place-items-center rounded-full ${u.role === "admin" ? "bg-primary/15 text-primary" : "bg-muted"}`}>{u.role === "admin" ? <ShieldCheck className="h-4 w-4" /> : <UsersIcon className="h-4 w-4" />}</div><div className="min-w-0 flex-1"><p className="truncate font-medium">{u.username}</p><p className="text-xs text-muted-foreground capitalize">{u.role === "admin" ? "admin" : (u.roleTemplate ?? "custom")}</p></div><Button size="sm" variant="ghost" onClick={() => setEditing(u)}><Pencil className="h-4 w-4" /></Button><Button size="sm" variant="ghost" disabled={u.role === "admin" && users.filter((x) => x.role === "admin").length === 1} onClick={() => { if (u.id === currentUser?.id) { toast.error("You cannot remove your own account"); return; } if (!window.confirm(`Remove user "${u.username}"?`)) return; removeUser(u.id); setUsersTick((n) => n + 1); toast.success("User removed"); }}><Trash2 className="h-4 w-4 text-destructive" /></Button></div>)}{users.length === 0 && <div className="p-6 text-center text-sm text-muted-foreground">No users yet.</div>}</div><p className="mt-3 text-[11px] text-muted-foreground">Users: {usersTick}</p></section></TabsContent>
      <TabsContent value="rackaccess" className="mt-0"><RackAccessSettings /></TabsContent>
      <TabsContent value="general"><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Pharmacy details</h2><p className="mb-4 text-xs text-muted-foreground">Appears on receipts and printed invoices.</p><div className="grid gap-4 md:grid-cols-2"><Field label="Pharmacy name"><Input value={s.pharmacyName} onChange={(e) => set("pharmacyName", e.target.value)} /></Field><Field label="Owner name"><Input value={s.ownerName} onChange={(e) => set("ownerName", e.target.value)} /></Field><Field label="Phone"><Input value={s.phone} onChange={(e) => set("phone", e.target.value)} /></Field><Field label="Email"><Input value={s.email} onChange={(e) => set("email", e.target.value)} /></Field><Field label="Address" className="md:col-span-2"><Input value={s.address} onChange={(e) => set("address", e.target.value)} /></Field></div></section><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Billing</h2><div className="mt-4 grid gap-4 md:grid-cols-3"><Field label="Tax % (applied automatically to every sale)"><Input type="number" value={s.taxPercent || ""} onChange={(e) => set("taxPercent", Number(e.target.value) || 0)} /></Field><Field label="Currency code"><Input value={s.currency} onChange={(e) => set("currency", e.target.value)} /></Field><Field label="Currency symbol"><Input value={s.currencySymbol} onChange={(e) => set("currencySymbol", e.target.value)} /></Field><Field label="Default max discount % (0 = unlimited)" className="md:col-span-3"><Input type="number" value={s.maxDiscount || ""} onChange={(e) => set("maxDiscount", Number(e.target.value) || 0)} /></Field></div><label className="mt-5 flex items-center justify-between gap-3 rounded-md border p-3"><div><p className="text-sm font-medium">Allow admin to change item price at checkout</p><p className="text-xs text-muted-foreground">Enabled by default. This changes the price for the current sale only.</p></div><Switch checked={s.allowCheckoutPriceChange !== false} onCheckedChange={(v) => set("allowCheckoutPriceChange", v)} /></label></section><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Printed bill</h2><div className="mt-4 grid gap-4"><Field label="Footer line 1 (thank-you message, max ~60 words)"><Input value={s.billFooter1} onChange={(e) => set("billFooter1", e.target.value.split(/\s+/).slice(0, 60).join(" "))} /></Field><Field label="Footer line 2 (return / exchange policy)"><Input value={s.billFooter2} onChange={(e) => set("billFooter2", e.target.value)} /></Field></div></section></TabsContent>
      <TabsContent value="workspace"><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Search</h2><div className="mt-4 max-w-xs"><Label className="mb-1.5 block text-xs font-medium text-muted-foreground">Default search by (top search bar)</Label><Select value={s.defaultSearchBy ?? "name"} onValueChange={(v) => set("defaultSearchBy", v as typeof s.defaultSearchBy)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="name">Medicine name</SelectItem><SelectItem value="generic">Generic name</SelectItem><SelectItem value="company">Company name</SelectItem></SelectContent></Select></div></section><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Pinned bottom panel</h2><div className="mt-4 flex flex-wrap items-center gap-6"><label className="flex items-center gap-3"><Switch checked={!s.pinPanelHidden} onCheckedChange={(v) => set("pinPanelHidden", !v)} /><span className="text-sm font-medium">Show pinned panel</span></label><Button size="sm" variant="outline" onClick={() => { mutate((d) => { d.settings.pinnedItems = []; }); toast.success("Pins cleared"); }}><Trash2 className="mr-1.5 h-4 w-4" /> Clear pins ({(s.pinnedItems ?? []).length})</Button></div><p className="mt-2 text-xs text-muted-foreground">Who can see and pin this panel is controlled per user under Users.</p></section><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Calculator</h2><label className="mt-4 flex items-center gap-3"><Switch checked={s.calculatorEnabled !== false} onCheckedChange={(v) => set("calculatorEnabled", v)} /><span className="text-sm font-medium">Show the calculator tab in the sidebar</span></label></section><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Auto save</h2><div className="mt-4 flex flex-wrap items-center gap-6"><label className="flex items-center gap-3"><Switch checked={s.autoSaveEnabled} onCheckedChange={(v) => set("autoSaveEnabled", v)} /><span className="text-sm font-medium">Enable auto save</span></label><div className="flex items-center gap-2"><Label className="text-sm">Interval</Label><Select value={String(s.autoSaveIntervalMinutes)} onValueChange={(v) => set("autoSaveIntervalMinutes", Number(v) as typeof s.autoSaveIntervalMinutes)}><SelectTrigger className="w-40"><SelectValue /></SelectTrigger><SelectContent>{[1,2,5,10,15].map((n)=><SelectItem key={n} value={String(n)}>{n} minute{n===1?"":"s"}</SelectItem>)}</SelectContent></Select></div></div></section></TabsContent>
      <TabsContent value="network"><DeploymentSettings /></TabsContent>
      <TabsContent value="notifications"><section className="surface-card mt-4 p-6"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><Bell className="h-4 w-4 text-primary" /><h2 className="font-display text-base font-semibold">Notification center</h2></div><p className="mt-1 text-xs text-muted-foreground">Choose which pharmacy events create notifications. This includes prescription follow-ups and stock warnings and works in Single Computer and Multi Computer modes.</p></div><Button size="sm" variant="outline" onClick={resetNotifications}>Restore defaults</Button></div><section className="mt-5 rounded-lg border bg-muted/10 p-4"><div className="mb-3 flex items-center gap-2"><Bell className="h-4 w-4 text-primary" /><div><h3 className="text-sm font-semibold">Quick notification controls</h3><p className="text-[11px] text-muted-foreground">These commonly used alerts are kept here with the full notification catalog.</p></div></div><div className="grid gap-3 md:grid-cols-2"><label className="flex items-center justify-between gap-3 rounded-md border bg-background p-3"><span className="text-sm">When a user deletes a medicine</span><Switch checked={s.notifyOnDeleteMedicine === true} onCheckedChange={(v)=>set("notifyOnDeleteMedicine",v)} /></label><label className="flex items-center justify-between gap-3 rounded-md border bg-background p-3"><span className="text-sm">When a user adds a medicine</span><Switch checked={s.notifyOnAddMedicine === true} onCheckedChange={(v)=>set("notifyOnAddMedicine",v)} /></label><label className="flex items-center justify-between gap-3 rounded-md border bg-background p-3"><span className="text-sm">When a user adds a customer</span><Switch checked={s.notifyOnAddCustomer === true} onCheckedChange={(v)=>set("notifyOnAddCustomer",v)} /></label><label className="flex items-center justify-between gap-3 rounded-md border bg-background p-3"><span className="text-sm">When a force sale is completed</span><Switch checked={s.notifyOnForceSale !== false} onCheckedChange={(v)=>set("notifyOnForceSale",v)} /></label><div className="rounded-md border bg-background p-3"><label className="flex items-center justify-between gap-3"><span className="text-sm">Large sale alert</span><Switch checked={s.notifyOnLargeSale === true} onCheckedChange={(v)=>set("notifyOnLargeSale",v)} /></label>{s.notifyOnLargeSale && <div className="mt-2"><Input type="number" placeholder="Threshold amount" value={s.largeSaleThreshold || ""} onChange={(e)=>set("largeSaleThreshold",Number(e.target.value)||0)} /></div>}</div></div></section><div className="relative mt-4"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input value={notificationSearch} onChange={(e)=>setNotificationSearch(e.target.value)} placeholder="Search notification events, e.g. new medicine, return, prescription…" className="pl-9" /></div><div className="mt-4 max-h-[60vh] overflow-auto rounded-md border divide-y">{notificationRows.map((type)=><label key={type} className="flex items-center justify-between gap-4 px-4 py-3 hover:bg-muted/40"><div><p className="text-sm font-medium">{NOTIFICATION_LABELS[type]}</p><p className="text-[11px] text-muted-foreground">{type}</p></div><Switch checked={notificationEnabled(type)} onCheckedChange={(v)=>setNotification(type,v)} /></label>)}{notificationRows.length===0&&<p className="p-6 text-center text-sm text-muted-foreground">No notification events match your search.</p>}</div><p className="mt-3 text-[11px] text-muted-foreground">Defaults are preserved for existing projects; disabled events never appear in the notification bell.</p></section></TabsContent>
      <TabsContent value="data"><section className="surface-card mt-4 p-6"><div className="mb-4 flex items-center gap-2"><HardDriveDownload className="h-4 w-4 text-primary" /><h2 className="font-display text-base font-semibold">Backup & data</h2></div><p className="mb-4 text-xs text-muted-foreground">Your data lives inside the application. A backup file is portable.</p><div className="grid gap-3"><div className="flex flex-wrap items-center justify-between gap-3 rounded-md border p-3"><div><p className="text-sm font-medium">Create backup (F5)</p><p className="text-xs text-muted-foreground">{s.autoBackupFolder ? `Saves to ${s.autoBackupFolder} and overwrites the previous backup.` : "Choose a folder once — later backups reuse it automatically."}</p></div><div className="flex gap-2"><Button size="sm" onClick={()=>void doBackupNow(false)} disabled={backupBusy}><HardDriveDownload className="mr-1.5 h-4 w-4" /> Backup now</Button><Button size="sm" variant="outline" onClick={()=>void doBackupNow(true)} disabled={backupBusy}><FolderOpen className="mr-1.5 h-4 w-4" /> Change location</Button></div></div><div className="flex flex-wrap items-center justify-between gap-3 rounded-md border p-3"><div><p className="text-sm font-medium">Load data</p><p className="text-xs text-muted-foreground">Restore everything from a backup file. Replaces current data.</p></div><Button size="sm" variant="outline" onClick={doLoadBackup} disabled={backupBusy}><HardDriveUpload className="mr-1.5 h-4 w-4" /> Load data</Button></div><div className="rounded-md border p-3"><div className="flex flex-wrap items-center justify-between gap-3"><label className="flex items-center gap-3"><Switch checked={!!s.autoBackupEnabled} onCheckedChange={toggleAutoBackup} /><span className="text-sm font-medium">Auto backup</span></label><div className="flex items-center gap-2"><Label className="text-sm">Every</Label><Select value={String(s.autoBackupIntervalHours ?? 24)} onValueChange={(v)=>set("autoBackupIntervalHours",Number(v))}><SelectTrigger className="w-40"><SelectValue /></SelectTrigger><SelectContent>{[1,6,12,24,48,72,168].map((h)=><SelectItem key={h} value={String(h)}>{h<24?`${h} hour${h===1?"":"s"}`:`${h/24} day${h===24?"":"s"}`}</SelectItem>)}</SelectContent></Select></div></div><div className="mt-3 flex flex-wrap items-center gap-3"><span className="text-xs text-muted-foreground">Folder: {s.autoBackupFolder ? <span className="font-medium text-foreground">{s.autoBackupFolder}</span> : "not set"}</span><Button size="sm" variant="outline" onClick={chooseAutoBackupFolder}><FolderOpen className="mr-1.5 h-4 w-4" /> Change location</Button></div></div><div className="rounded-md border p-3"><label className="flex items-center gap-3"><Switch checked={!!s.backupPasswordEnabled} onCheckedChange={(v)=>set("backupPasswordEnabled",v)} /><span className="text-sm font-medium">Password protect backup files</span></label><p className="mt-2 text-xs text-muted-foreground">Backups are encrypted with this password. Loading such a backup asks for it first.</p>{s.backupPasswordEnabled&&<div className="mt-3 max-w-sm"><Field label="Backup password"><PasswordInput value={s.backupPassword??""} onChange={(e)=>set("backupPassword",e.target.value)} /></Field></div>}</div></div></section></TabsContent>
      <TabsContent value="shortcuts"><KeyboardSettingsPanel /><section className="surface-card mt-4 p-6"><div className="mb-4 flex items-center gap-2"><Keyboard className="h-4 w-4 text-primary" /><h2 className="font-display text-base font-semibold">Keyboard shortcuts</h2></div><div className="flex flex-wrap items-center gap-3"><label className="flex items-center gap-3"><Switch checked={s.tabShortcutsEnabled !== false} onCheckedChange={(v)=>set("tabShortcutsEnabled",v)} /><span className="text-sm font-medium">Tab shortcuts</span></label><Button size="sm" variant="outline" className="ml-auto" onClick={()=>{mutate((d)=>{d.settings.tabHotkeys={};});toast.success("Shortcuts restored to defaults");}}><RotateCcw className="mr-1.5 h-3.5 w-3.5" /> Set defaults</Button></div><div className="mt-4 grid gap-2 rounded-md border p-3 sm:grid-cols-2">{visibleNavItems(s,currentUser).map((t,i)=>{const combo=effectiveHotkey(t.to,i,s.tabHotkeys);const isCapturing=capturing===t.to;return <div key={t.to} className="flex items-center gap-2"><span className="flex-1 text-sm">{t.label}</span><Button size="sm" variant={isCapturing?"default":"outline"} className="w-40 justify-center font-mono text-xs" onClick={()=>setCapturing(isCapturing?null:t.to)}>{isCapturing?"Press key…":combo||"Not set"}</Button><Button size="icon" variant="ghost" onClick={()=>mutate((d)=>{const map={...(d.settings.tabHotkeys??{})};delete map[t.to];d.settings.tabHotkeys=map;})}><RotateCcw className="h-3.5 w-3.5" /></Button></div>})}</div></section><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Quick action shortcuts</h2><p className="mb-3 text-sm text-muted-foreground">Open a create panel from anywhere.</p><div className="grid gap-2 sm:grid-cols-2">{QUICK_ACTIONS.map((a)=><div key={a.id} className="flex items-center justify-between rounded-md border px-3 py-2"><span className="text-sm">{a.label}</span><Button size="sm" variant={capturingAction===a.id?"default":"outline"} className="font-mono text-xs" onClick={()=>setCapturingAction(a.id)}>{capturingAction===a.id?"Press keys…":effectiveActionHotkey(a.id,s.actionHotkeys)}</Button></div>)}</div></section></TabsContent>
      <TabsContent value="danger"><section className="surface-card mt-4 p-6"><h2 className="font-display text-base font-semibold">Invoice counter</h2><p className="mb-3 text-sm text-muted-foreground">Next invoice #: <span className="font-mono">{s.invoiceCounter??1000}</span> — resetting keeps all bills.</p><Button size="sm" variant="outline" onClick={()=>{if(!window.confirm("Reset the invoice counter back to 1000? Bills are kept."))return;mutate((d)=>{d.settings.invoiceCounter=1000;});toast.success("Invoice counter reset to 1000");}}>Reset invoice counter</Button></section><section className="surface-card mt-4 border-destructive/40 p-6"><div className="mb-4 flex items-center gap-2"><AlertTriangle className="h-4 w-4 text-destructive" /><h2 className="font-display text-base font-semibold text-destructive">Danger zone</h2></div><div className="grid gap-4"><div className="flex flex-wrap items-center justify-between gap-3 rounded-md border p-3"><div><p className="text-sm font-medium">Clear all bill history</p><p className="text-xs text-muted-foreground">Permanently deletes every sale/invoice from this project.</p></div><Button variant="destructive" size="sm" onClick={clearBillHistory}><Trash2 className="mr-1.5 h-4 w-4" /> Clear bill history</Button></div><div className="flex flex-wrap items-center justify-between gap-3 rounded-md border p-3"><div><p className="text-sm font-medium">Reset pharmacy setup</p><p className="text-xs text-muted-foreground">Requires admin password. Returns to first-time setup. Data file on disk is not deleted.</p></div><Button variant="outline" size="sm" onClick={()=>setShowReset(true)}><RotateCcw className="mr-1.5 h-4 w-4" /> Reset setup</Button></div></div></section></TabsContent>
    </Tabs>
    {showNew&&<UserDialog onClose={()=>setShowNew(false)} onSaved={()=>{setShowNew(false);setUsersTick((n)=>n+1);}} />}{editing&&<UserDialog existing={editing} onClose={()=>setEditing(null)} onSaved={(u)=>{setEditing(null);setUsersTick((n)=>n+1);if(u&&u.id===currentUser?.id)setSessionUser(u);}} />}<ResetSetupDialog open={showReset} onOpenChange={setShowReset} onDone={()=>navigate({to:"/"})} />
  </div>;
}

function UserDialog({ existing, onClose, onSaved }: { existing?: StoredUser; onClose: () => void; onSaved: (u?: StoredUser) => void; }) {
  const isMultiComputer = (readInstall()?.deployMode ?? "single") !== "single";
  const [username,setUsername]=useState(existing?.username??""); const [password,setPassword]=useState(""); const [role,setRole]=useState<"admin"|"user">(existing?.role??"user");
  const initialTemplate: RoleTemplate=existing?.roleTemplate??(existing?"custom":"seller"); const [template,setTemplate]=useState<RoleTemplate>(initialTemplate); const [templatePerms,setTemplatePerms]=useState<UserPermissions>(existing?.permissions??permissionsForTemplate(initialTemplate)); const [userMaxDiscount,setUserMaxDiscount]=useState<number>(existing?.maxDiscount??0);
  function pickTemplate(next:RoleTemplate){setTemplate(next);setTemplatePerms(permissionsForTemplate(next));}
  async function submit(){ if(!username.trim()){toast.error("Username required");return;} if(!existing&&password.length<4){toast.error("Password must be at least 4 characters");return;} const finalRole:"admin"|"user"=isMultiComputer?"user":role; const finalPerms:UserPermissions=finalRole==="admin"?defaultPermissions("admin"):templatePerms; const finalMaxDiscount=finalRole!=="admin"&&finalPerms.applyDiscount?(userMaxDiscount||0):undefined; if(existing){let saltHex=existing.saltHex,hashHex=existing.hashHex;if(password){const h=await hashPassword(password);saltHex=h.saltHex;hashHex=h.hashHex;}const updated:StoredUser={...existing,username:username.trim(),role:finalRole,saltHex,hashHex,permissions:finalPerms,maxDiscount:finalMaxDiscount,roleTemplate:finalRole==="admin"?undefined:template};upsertUser(updated);toast.success("User updated");onSaved(updated);}else{const u=await createUser({username,password,role:finalRole,permissions:finalRole==="admin"?undefined:finalPerms});if(finalRole!=="admin"&&finalPerms.applyDiscount)u.maxDiscount=userMaxDiscount||0;if(finalRole!=="admin")u.roleTemplate=template;upsertUser(u);toast.success("User created");onSaved(u);} }
  const showTemplate=isMultiComputer||role!=="admin"; const setPermission=(key:keyof UserPermissions,value:boolean)=>setTemplatePerms((p)=>({...p,[key]:value}));
  const renderPanel=(panel:PermissionPanel)=>{ const enabled=templatePerms[panel.key]===true; return <div key={String(panel.key)} className="flex flex-wrap items-center gap-x-5 gap-y-2 rounded-md border px-3 py-2.5"><label className="flex min-w-[190px] items-center gap-2 text-sm font-medium"><Checkbox checked={enabled} onCheckedChange={(v)=>setPermission(panel.key,v===true)} /><span>{panel.label}</span></label>{enabled&&panel.actions&&<div className="flex flex-wrap items-center gap-x-4 gap-y-1.5 border-l pl-4">{panel.actions.map((a)=><label key={String(a.key)} className="flex items-center gap-1.5 text-xs"><Checkbox checked={templatePerms[a.key]===true} onCheckedChange={(v)=>setPermission(a.key,v===true)} /><span>{a.label}</span></label>)}</div>}</div>; };
  return <Dialog open onOpenChange={(o)=>!o&&onClose()}><DialogContent className="max-w-3xl max-h-[88vh] overflow-y-auto"><DialogHeader><DialogTitle>{existing?"Edit user":"Add user"}</DialogTitle></DialogHeader><div className="grid gap-4"><Field label="Username"><Input value={username} onChange={(e)=>setUsername(e.target.value)} /></Field><Field label={existing?"New password (leave blank to keep current)":"Password"}><PasswordInput value={password} onChange={(e)=>setPassword(e.target.value)} /></Field>{!isMultiComputer&&<Field label="Account type"><Select value={role} onValueChange={(v)=>setRole(v as "user"|"admin")}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="user">User</SelectItem><SelectItem value="admin">Admin (full access)</SelectItem></SelectContent></Select></Field>}{showTemplate&&<><Field label="Role template"><Select value={template} onValueChange={(v)=>pickTemplate(v as RoleTemplate)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="seller">{ROLE_TEMPLATE_LABELS.seller}</SelectItem><SelectItem value="custom">{ROLE_TEMPLATE_LABELS.custom}</SelectItem><SelectItem value="manager">{ROLE_TEMPLATE_LABELS.manager}</SelectItem></SelectContent></Select></Field><div className="rounded-md border p-3"><div className="mb-3 flex items-center justify-between"><p className="text-xs font-semibold text-muted-foreground">{template === "custom" ? "Custom permissions" : template === "seller" ? "Seller permissions" : "Manager permissions"}</p><span className="text-[11px] text-muted-foreground">Tick a panel, then choose its actions on the same line.</span></div><div className="grid gap-2">{PERMISSION_PANELS.map(renderPanel)}</div>{templatePerms.applyDiscount&&<div className="mt-3 max-w-sm"><Field label="Max discount % for this user (0 = unlimited)"><Input type="number" value={userMaxDiscount||""} onChange={(e)=>setUserMaxDiscount(Number(e.target.value)||0)} /></Field></div>}</div></>}</div><DialogFooter><Button variant="ghost" onClick={onClose}>Cancel</Button><Button onClick={submit}>{existing?"Save changes":"Create user"}</Button></DialogFooter></DialogContent></Dialog>;
}
function Field({label,children,className}:{label:string;children:React.ReactNode;className?:string}){return <div className={className}><Label className="mb-1.5 block text-xs font-medium text-muted-foreground">{label}</Label>{children}</div>}
